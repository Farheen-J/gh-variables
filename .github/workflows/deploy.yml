name: Deployment
on:
  push:
    branches:
      - main
      - dev
env:                # One way to declare env variable, this can be job specific as well
  MONGODB_CLUSTER_ADDRESS: gh-env-secrets.rla2fx2.mongodb.net # Name of the DB does not change
  MONGODB_USERNAME: farheenjamadar158
  MONGODB_PASSWORD: HjvLehh3hzGn4Lvl
  PORT: 8080
jobs:
  test:
    #env:
      #MONGODB_DB_NAME:       # This is job specific
    environment: testing
    runs-on: ubuntu-latest
    steps:
      - name: Get Code
        uses: actions/checkout@v3
      - name: Cache dependencies
        uses: actions/cache@v3
        with:
          path: ~/.npm
          key: npm-deps-${{ hashFiles('**/package-lock.json') }}
      - name: Install dependencies
        run: npm ci
      - name: Run server
        run: npm start & npx wait-on http://127.0.0.1:$PORT    # Environment variables can be used in the pipeline as well, this is Linux syntax
      - name: Run tests
        run: npm test
      - name: Output information
        run: |    # Multiple syntax
          echo "MONGODB_CLUSTER_ADDRESS: ${{ env.MONGODB_CLUSTER_ADDRESS }}" 
          echo "MONGODB_CLUSTER_ADDRESS: $MONGODB_CLUSTER_ADDRESS"
  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Output information
        run: |        
          echo "..."
